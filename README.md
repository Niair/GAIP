# GAIP

      git status
      
      git add .
      
      git commit -m "Day-
      
      git push -u origin main

# once
pip install uv

# per project
uv venv -p 3.11
.venv\Scripts\activate
uv pip install -r requirements.txt
